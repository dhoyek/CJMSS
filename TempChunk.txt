    },    calculateVolumeRealTime: function (formContext) {        try {            var L = formContext.getAttribute("pdg_length");            var W = formContext.getAttribute("pdg_width");            var H = formContext.getAttribute("pdg_height");            var out = formContext.getAttribute("pdg_volume");            if (!out) return;            var l = L ? (L.getValue() || 0) : 0;            var w = W ? (W.getValue() || 0) : 0;            var h = H ? (H.getValue() || 0) : 0;            var v = (l > 0 && w > 0 && h > 0) ? (l * w * h) / 1e9 : 0;            out.setValue(v);            if (v > 0) {                this.showNotification(formContext, "Volume calculated: " + v.toLocaleString() + " mm³", "INFO", "volume_calculated");            }        } catch (e) {            console.error("Error calculating volume:", e);